export enum tagTypes {
  user = "user",
  profile = "profile",
  service = "service",
  booking = "booking",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.profile,
  tagTypes.service,
  tagTypes.booking,
];
